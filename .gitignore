node_modules/
# Без слэша тоже: со слэшем правило ловит только КАТАЛОГ, и симлинк с этим
# именем проскакивает в git add -A. 15.08.2026 так в main уехал node_modules,
# указывающий сам на себя (/Users/.../spirits_back/node_modules) — на сервере
# git reset --hard создавал бы ссылку на несуществующий путь с чужой машины.
node_modules
dist/
.env
*.log
.worktrees/
scripts/test-server.env.local
test-results/
vk.ads.txt
