/**
 * Требование к ФОРМЕ ответа ассистента — общее для всех путей сборки промпта.
 *
 * Живёт отдельным файлом, потому что промпт ассистента собирается в трёх местах
 * (`chat.service.ts`: Маша напрямую, остальные через релей `streamUniversalAgent`,
 * и текстовый ответ для голосового звонка `generateAgentReplyWithCharge`).
 * Правило языка уже приходилось чинить трижды из-за той же разбросанности —
 * повторять историю с формой ответа незачем.
 *
 * Ставится ПЕРЕД языковой директивой: последняя строка промпта должна остаться
 * языковой (см. `prompt-language-tail.spec.ts` и комментарии в chat.service.ts).
 */
export const RESPONSE_STYLE_RULE =
  `--- ФОРМА ОТВЕТА ---
• Ответы держи сфокусированными, короткими и сжатыми.
• Оговорки и дисклеймеры давай коротко: основное место отдавай главному ответу.
• Когда просят объяснить — давай высокоуровневое резюме, если подробное объяснение не запрошено явно.`;
