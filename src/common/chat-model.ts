/**
 * Модель, на которой отвечают пользовательские ассистенты.
 *
 * ПРИМЕНЯЕТСЯ К ТРЁМ ПУТЯМ ЧАТА:
 *   - веб — все ассистенты кроме Маши, через релей r.linkeon.io
 *     (chat.service, поле `model` в multipart-запросе к file-agent)
 *   - Telegram-боты (tg-router, локальный ClaudeCliService)
 *   - Маша (chat.service, локальный ClaudeCliService)
 *
 * НЕ ПРИМЕНЯЕТСЯ к служебным вызовам (профиль из чата, саппорт, бэклог,
 * задачи, календарь, сценарии SMM, neo4j): они сидят на haiku осознанно, это
 * не диалог с пользователем. И не применяется к пингам мониторинга — те просят
 * haiku нарочно, потому что проверяют живость пути, а не качество ответа.
 *
 * Откуда взялся пин. До 23.09.2026 все три пути просили `'default'` —
 * рекомендуемую моделью CLI. Это давало бесплатный автодаунгрейд: при упоре в
 * недельный лимит подписки CLI сам съезжал на Sonnet, и ассистент продолжал
 * отвечать, пусть и слабее. Решение владельца 23.09.2026 — пин на конкретную
 * версию: `'default'` выбирает Anthropic, и однажды он поедет без спроса.
 *
 * ЧЕМ ЗА ЭТО ПЛАТИМ, ЯВНО. Прибитый id НЕ даунгрейдится. При исчерпании лимита
 * подписки вызов падает, а не деградирует, — в вебе это «ассистент молчит», в
 * телеге бот просто не отвечает. Ровно этот сценарий раньше описывали
 * комментарии у tg-router и chat.service как причину держать `'default'`;
 * владелец ознакомлен и выбрал пин. Не возвращать к `'default'` молча, это не
 * забытый TODO.
 *
 * ЧТО ДЕЛАТЬ, ЕСЛИ АССИСТЕНТЫ ЗАМОЛЧАЛИ. Значение читается из env, чтобы откат
 * не требовал выката: `CHAT_MODEL=default` в `.env` на проде + `pm2 restart
 * linkeon-api` возвращают прежнее поведение с автодаунгрейдом за минуту.
 * Симптом, по которому это узнаётся, — 400/429 от CLI при живом релее.
 *
 * ВНИМАНИЕ, ВЕРСИЯ CLI. Новые модели гейтятся версией бинаря, а не аккаунтом:
 * claude-opus-5-5 требует Claude Code >= 2.1.280, более старый отдаёт
 * `400 ... does not support this model`. Значит, при смене значения проверять
 * версию на ОБОИХ хостах — на проде (`/usr/bin/claude`, его зовёт
 * ClaudeCliService) и на релее (`~/.local/bin/claude`, его зовёт file-agent).
 * Релей выкатывается отдельно от deploy.sh.
 *
 * ВНИМАНИЕ, БЕЛЫЙ СПИСОК РЕЛЕЯ. Значение уезжает в argv `claude --model`, и
 * file-agent принимает только то, что перечислено в его `MODELS`
 * (server.mjs). Незнакомое значение он молча заменит на `default` — то есть
 * пин тихо перестанет действовать в вебе, оставшись в силе в телеге. Меняешь
 * здесь — добавляй и туда.
 */
export const CHAT_MODEL = process.env.CHAT_MODEL || 'claude-opus-5-5';

/**
 * Модель для пингов мониторинга (synthetic-зонд, проба живости пути).
 *
 * Вынесена рядом, чтобы при смене CHAT_MODEL её случайно не подняли заодно:
 * обвязка Claude Code (системный промпт CLI + определения MCP-тулов, ~47k
 * токенов) грузится на каждый вызов независимо от содержания хода, поэтому
 * «ответь одним словом ок» на Opus стоит ~$0.20 против ~$0.02 на haiku, а
 * таких пингов ~150 в сутки.
 */
export const PROBE_MODEL = 'haiku';
