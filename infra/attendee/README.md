# Attendee — мост в встречи Google Meet

Итоговый отчёт для команды — замеры, ограничения, что нужно для прода:
[`docs/meeting-meet-result.md`](../../docs/meeting-meet-result.md). Здесь —
рабочие подробности: установка, обязательная правка, разбор каждого прогона.

Attendee держит в встрече Chrome-бота и обменивается с нами звуком по
вебсокету. Через него ассистент попадает в площадки без LiveKit.

Живёт на **отдельном хосте**. Не на проде: каждый бот — полный Chrome со
страницей Meet, а на `212.113.106.202` уже API, Postgres, Redis, Neo4j и
LiveKit; конкуренция за CPU ударит по SFU.

## ⚠️ Читать первым: Celery-режим не годится для продакшена

Дословно из README проекта (проверено 07.09.2026):

> By default, Attendee launches bots as Celery tasks. This mode is convenient
> for local development and testing, **but it is not suitable for production**
> for two reasons.
>
> The correct way to run Attendee in production is with Kubernetes, where each
> bot runs in its own isolated pod. Enable this by setting the environment
> variable: `LAUNCH_BOT_METHOD="kubernetes"`
>
> **Our paid self-hosting plan** includes official directions and support for
> running Attendee with Kubernetes.

Две причины непригодности:

1. **Перетекание звука.** Несколько ботов в одном Celery-контейнере делят
   аудиоустройства, и звук разных встреч может смешаться. В переговорах с
   клиентами это неприемлемо — не «шум», а утечка одной встречи в другую.
2. **Рестарт убивает живых ботов.** Деплой или перезапуск контейнера
   выбрасывает бота из идущей встречи.

Kubernetes мы не держим, а официальные инструкции к нему — в платном плане.
Поэтому решение на v1 (см. ниже) — **жёсткий потолок в одну одновременную
встречу**: при одном боте перетекать нечему.

Потолок держится с нашей стороны, а не надеждой:

- диапазон портов в `voice-host/.env` сужается до одного:
  `ATTENDEE_WS_PORT_MIN=8140`, `ATTENDEE_WS_PORT_MAX=8140`. Второе
  одновременное задание не найдёт свободного порта и честно откажет;
- та же регулярка в `nginx-attendee.conf` сужается до `8140`;
- на бэкенде стоит явная проверка: `MeetingService.join()` считает встречи
  Meet в эфире **до** создания записи звонка и отвечает `409` с
  `reason: 'meet_busy'`. Без неё второй пользователь получал бы невнятный
  отказ через несколько секунд — когда воркер не найдёт свободного порта.

Потолок задан константой `MEET_CONCURRENCY_LIMIT`, а не жёсткой единицей.
**Поднимать её можно только вместе с переходом на изоляцию ботов по поду и
одновременно с диапазоном портов.** Иначе потолок снимется, а утечка звука
между переговорами разных клиентов появится — тихо.

Когда одной встречи станет мало — либо платный план и Kubernetes, либо по
хосту Attendee на встречу. Это решение владельца, не техническое.

## Деплой во время живой встречи: окно короче встречи

`deploy.sh` перед выкаткой ждёт, пока опустеет эфир (`wait_for_calls_drain`),
и встречу Meet он **видит**: ручка `/webhook/voice-call-status/active` считает
все `voice_calls` со статусами `dialing`/`active`, без фильтра по провайдеру.

Но ждёт он не бесконечно. `CALL_DRAIN_SECONDS` по умолчанию **900 секунд**, и
после них скрипт печатает предупреждение и **идёт дальше**. Окно выбиралось под
звонки один-на-один — в комментарии там так и написано, «наш собственный потолок
звонка — час». У встречи потолок **два часа** (`HARD_CAP_MS` в
`voice-host/src/occupancy.ts`), а реапер добивает её через 130 минут.

Значит деплой на шестнадцатой минуте перезапустит воркер и выбросит бота из
идущей встречи. Это не специфика Meet: то же верно для своих комнат и Taler ID,
просто там оно до сих пор не всплывало.

Что с этим делать, пока встречи редки:

- перед выкаткой смотреть глазами:
  `SELECT id, provider, started_at FROM voice_calls WHERE status IN ('dialing','active')`;
- если встреча идёт и выкатка не пожарная — подождать;
- если ждать нельзя, поднять окно на этот запуск:
  `CALL_DRAIN_SECONDS=7200 bash scripts/deploy.sh`.

Поднимать дефолт до двух часов **не стали**: это заблокировало бы любую
выкатку на всё время чужой встречи, включая срочные фиксы. Решение про
постоянное окно — за владельцем, когда встречи перестанут быть редкостью.

## Сквозной прогон на стенде 09.09.2026: цепочка держится

Attendee поднят на тестовой ноде, стенд переведён на ветку `feat/meeting-meet`.
Прогон с **несуществующим** кодом встречи — чтобы проверить весь путь, кроме
собственно звука:

```
статус=failed  бот=bot_ImG1uiz1u9UYMNBl  причина=Звонок не состоялся: бот Attendee: fatal_error
[attendee] жду звук на :8140, callId=03c3aad7-…
[meet] бот в состоянии fatal_error до подключения звука — выходим
```

Подтверждено на живой системе: вход с `provider=meet`, диспатч в комнату,
занятие порта воркером, вызов подписанной ручки `meet-bot`, создание бота
через API Attendee, приём им наших адресов по TLS, реальная попытка входа
(события бота `join_requested` → `could_not_join_meeting`), доставка вебхука,
**проверка подписи**, передача события в комнату по дата-каналу, выход воркера
с настоящей причиной и запись отказа в `voice_calls`.

**Проверка подписи подтверждена не тестом, а трафиком:** Attendee записал
четыре попытки доставки, и у всех проставлен `succeeded_at` — что невозможно,
если бы ручка отвечала 401.

Звук в обе стороны на этом прогоне не проверялся: для этого нужна живая
встреча и человек, который впустит бота. Проверен отдельно — ниже.

### Четыре дефекта, которые нашёл только запуск

Все четыре были невидимы для 1398 тестов бэкенда и 111 тестов воркера.

| Дефект | Чем был опасен | Почему тесты молчали |
|---|---|---|
| Ключ подписи брался как base64-**текст**, а не его байты | Ни один вебхук не прошёл бы проверку. Присутствие не приходило бы вовсе, гейт по имени срывался бы в solo, ассистент отвечал на каждую реплику | Золотой вектор использовал секрет-строку с обеих сторон: он проверял канонизацию, а не обращение с ключом |
| Контроллер входа не передавал провайдера `meet` | Фича недостижима через API: встреча уходила искать свою комнату и получала 404 | Все тесты зовут `MeetingService` напрямую, минуя контроллер |
| `attachBot` не убирал бота при отказе после его создания | Chrome остаётся в переговорах клиента навсегда: `external_bot_id` пуст, реапер такого не найдёт | Ловушка была закрыта в `join()`, а `attachBot` — отдельный метод. Сценарий **произошёл вживую**, когда миграция ещё не была накатана |
| Обработчик `DataReceived` регистрируется ПОСЛЕ ожидания звука | Провал входа бота виден только через двухминутный таймаут и под чужой причиной («звук не подключился») | Порядок регистрации обработчиков в юнит-тесте не воспроизводится |

### Что ещё выяснилось

- **Attendee требует TLS на обоих обратных каналах** и проверяет валидатором:
  `webhooks: does not match '^https://.*'`, `websocket_settings.audio.url:
  URL must start with wss://`. Прод-схема с nginx — не удобство, а
  единственно возможная форма.
- **Секрет вебхука двоичный.** Интерфейс отдаёт его в base64, подписывает
  Attendee декодированными байтами.
- **Состояния бота**: реальный набор в `bots/models.py`, `BotStates`. Комната
  ожидания — `waiting_room`, и она НЕ отказ. Состояний `denied_entry` и
  `removed_from_meeting`, которые были в нашем коде, у Attendee не существует.
- **Запись по умолчанию — mp4 в S3.** Отключается `recording_settings:
  {format: 'none'}`, что и сделано: транскрипт мы ведём сами, а видео
  переговоров клиента в чужом хранилище — лишняя утечка.
- **Миграции на стенд надо накатывать руками** (`psql -f`), общий
  `npm run migrate` застревает на `base/001`. Забытая миграция и вскрыла
  третий дефект из таблицы выше.

## Живая встреча 09.09.2026: звук прошёл, встреча оборвалась на 95-й секунде

Владелец создал встречу, кинул ссылку в чат, впустил бота из комнаты ожидания.

Что подтвердилось:

- бот вошёл под именем «Роман · ассистент Дмитрия», состояния
  `joining` → `joined_recording` приехали вебхуками;
- звук ОТ встречи дошёл: **9244 куска** за ~185 секунд, ровно тем ритмом,
  который держит микшер;
- Роман говорил: сегмент **12,0 с** ушёл в вебсокет;
  ⚠️ **«слышен во встрече» здесь было моей догадкой, а не проверкой.** Владелец
  этого не подтверждал, а третий прогон 09.09.2026 показал обратное: звук от
  нас доходит до Attendee, но во встрече его не слышно — микрофон бота остаётся
  выключенным (см. раздел про обязательную локальную правку выше);
- транскрипт, резюме и списание — как на своих встречах, отдельного кода не
  потребовалось;
- бот убран из встречи по выходу, Chrome не остался висеть.

И три дефекта, которых не показал ни один прогон на стенде.

| Дефект | Как выглядел во встрече | Почему тесты молчали |
|---|---|---|
| События состава уезжали в пустоту: обработчик `meet_*` стоял ПОСЛЕ ожидания звука, а оно длится до двух минут | `[гейт] участников: 0 → строгий гейт` — Роман весь разговор считал, что во встрече никого. `meetingFirstHuman` не дёрнулся: звонок остался `dialing` и запер владельцу следующий вход до реапера (130 минут) | Порядок регистрации обработчиков в юнит-тесте не воспроизводится. Тот же класс дефекта, что четвёртый в таблице выше — исправление там было половинным: гонку с провалом входа закрыло, состав нет |
| Хаб отдавал соединение наружу ровно один раз, поэтому переподключение Attendee получало отказ | Вебсокет оборвался на 95-й секунде, Attendee постучался снова через мгновение — `[attendee] соединение не для нас` — и встреча кончилась на середине разговора | Тест проверял «дождались соединения», а второго соединения в нём не было вовсе |
| После обрыва задание не уходило из комнаты | Тикало тишиной **ещё четырнадцать минут**, держа единственный порт под Meet, и вышло только по вердикту `never_started`. С исправленным составом вердикт был бы `stay` — и задание висело бы до двухчасового потолка | `session.close()` в тестах выглядит как конец работы; в задании @livekit/agents конец — это `room.disconnect()` |

Исправлено в `fix(voice-host): встреча Meet переживает обрыв звука и не теряет
состав`: обработчик `meet_*` теперь один и регистрируется сразу после
`ctx.connect()`; сокетом владеет хаб, и вход с выходом сессии работают через
него, не замечая переподключений; наружу выходит только «бот не вернулся за
полторы минуты», и эта ветка, как и терминальное состояние бота, разъединяет
комнату. Добавлены тесты на переподключение, на потерю по истечении окна и на
подписку поверх уже случившейся потери.

**Причина самого обрыва на 95-й секунде не установлена.** `proxy_read_timeout`
у nginx — 7200 с, звук в этот момент шёл непрерывно. Как страховка от таймаута
по бездействию добавлен пинг раз в 30 секунд: звук ОТ Attendee идёт всё время,
а наш конец молчит, пока ассистент не говорит. Если обрыв повторится — смотреть
логи `attendee-worker` в момент разрыва, там ведётся его сторона вебсокета.

### Второй прогон 09.09.2026: что показал разбор

Вход запускался дважды, и оба раза бота никто не впустил — но именно это
объяснило обрыв из первого прогона и вскрыло ещё три дефекта.

**Обрыв на 95-й секунде был не сетевым.** В логе `attendee-worker` видно
всё: в 15:32:13 Meet отдал хозяина встречи со `status: 8`, Attendee счёл это
уходом (`only_one_participant_in_meeting_at set to …`), а через 60 секунд
одиночества сам нажал «выйти» — `Auto-leaving meeting because there was only
one participant in the meeting for 60 seconds`,
`AUTO_LEAVE_ONLY_PARTICIPANT_IN_MEETING`. Мы видели только закрывшийся
вебсокет. Дефолты автовыхода Attendee (`bots/automatic_leave_configuration.py`)
строже наших правил из `voice-host/src/occupancy.ts` и потому решали за нас,
поэтому теперь бот создаётся с явными `automatic_leave_settings`: одиночество
300 с вместо 60, тишина 1800 с вместо 600, потолок аптайма 7500 с — чуть выше
нашего двухчасового `HARD_CAP_MS`, чтобы причину называли мы, но Chrome не мог
пережить воркер.

| Дефект второго прогона | Как выглядел | Исправление |
|---|---|---|
| Окно ожидания впуска — 120 с | Бот отстучался в комнату ожидания, а мы сдались раньше, чем его впустили: звонок ушёл в `failed` с причиной «звук так и не подключился», хотя со встречей всё было в порядке | `CONNECT_TIMEOUT_MS` = 960 с — чуть больше, чем `waiting_room_timeout_seconds` у Attendee (900), чтобы первым сдавался он и называл причину |
| `removeBot` не понимал завершённого бота | Attendee отвечает на `leave` **400**, а не 404: «Event leave_requested not allowed when bot is in state ended». Читалось как «состояние неизвестно», и реапер перебирал четыре мёртвых бота каждые 5 минут, не очищая `external_bot_id` никогда | На 400 спрашиваем состояние и убираем запись только на терминальном; на `joining` и в комнате ожидания по-прежнему `null` — такой бот ещё может войти. Проверено на стенде: первый же тик реапера очистил все четыре записи |
| Причина отказа была бессмысленной | Оба прогона дали одну строку «бот Attendee: fatal_error» — и для «никто не нажал Впустить», и для упавшего Chrome. Настоящая причина лежала в вебхуке: `event_type: could_not_join_meeting`, `event_sub_type: request_to_join_denied` | Код едет по дата-каналу полем `sub`, воркер переводит его в человеческую строку («во встречу так и не впустили») и оставляет код в скобках. Незнакомый код отдаётся как есть — список подтипов у Attendee растёт |

Ранние исправления при этом подтвердились работой: второй бот получил
`fatal_error` на 616-й секунде, и воркер вышел **сразу**, с настоящей
причиной, — прежняя редакция досидела бы всё окно ожидания и сообщила бы
«звук не подключился».

**Сколько ждёт Attendee, если бота не впускают:** ровно 10 минут, потом
`No one responded to our request to join (timeout)` →
`UiRequestToJoinDeniedException` → `fatal_error` с подтипом
`request_to_join_denied`. Замерено 09.09.2026: стук в 16:18:59, отказ в
16:29:02.

## ⚠️ Обязательная локальная правка: микрофон бота в Google Meet

**Без неё ассистента во встрече не слышно вовсе.** Правка —
`patches/mic-label-tolerant.py`, применять после каждого обновления Attendee:

```bash
python3 patches/mic-label-tolerant.py ~/attendee
cd ~/attendee && docker compose -f dev.docker-compose.yaml restart attendee-worker-local
```

Что происходит без неё. Наш звук доходит до Attendee и проигрывается в
виртуальный микрофон страницы — это видно в логе воркера
(`RealtimeAudioOutputManager: Audio thread started`, а «Audio thread exited»
ровно через длину реплики). Но `playPCMAudio()` перед проигрыванием зовёт
`ensureMicOn()`, а тот искал кнопку так:

```js
document.querySelector('button[aria-label="Turn on microphone"]')
```

**Дело в теге.** Кнопка микрофона во встрече — не `<button>`, а `div` с
`role="button"`; на нём же живёт `data-is-muted`. Питонья часть Attendee
перечисляет оба тега (`div[aria-label="Turn off microphone"],
button[aria-label="Turn off microphone"]` в `google_meet_ui_methods.py`) и
потому микрофон перед входом глушит успешно, а JS искал только `button` — и не
находил ничего: микрофон бота оставался выключенным, участники слышали тишину.

Правка ищет среди любых элементов, а подпись сверяет терпимо — по началу
`aria-label`, по началу `data-tooltip` и по `data-is-muted`. Подпись в замере
совпала точно, без горячей клавиши: решающим был отказ от привязки к тегу, а
терпимость к подписи оставлена запасом на смену языка интерфейса.

Правка живёт в нашем репозитории, а не в форке: форк Attendee — это второй
источник правды и обязанность его вести. Как только апстрим исправит поиск
кнопки, скрипт нужно удалить.

### Третий прогон 09.09.2026: два дефекта под живым разговором

Бот вошёл, состав приехал, звонок ушёл из `dialing` — и на 229-й секунде
ассистент покинул живую встречу. Владелец в этот момент из встречи не выходил
и Романа не слышал (см. правку микрофона выше).

| Дефект | Как выглядел | Исправление |
|---|---|---|
| Мы верили одному событию `leave` | Meet отдал сидящего человека со `status: 8` («unknown» — этого кода нет даже в таблице статусов самого Attendee, где расписаны 1, 6 и 7). Attendee переводит в уход любой статус кроме `in_meeting`, состав стал нулевым, вердикт `empty` — и ассистент ушёл через пять секунд | Выдержка полторы минуты перед вердиктом «опустела» (для своих комнат ноль: LiveKit о составе не врёт) плюс речь как доказательство присутствия — заговоривший возвращается и в состав, и в правила выхода |
| Бот переживал звонок | Воркер завершился, а бот остался сидеть в переговорах владельца и одиннадцать раз постучался в уже закрытый порт (502 в его логе). Убрал его реапер — через несколько минут | Бот убирается на КАЖДОМ завершении, и в `complete()`, и в `fail()`; правило переехало в `VoiceCallService.releaseBot`, реапер зовёт его же |

## Живая встреча 10.09.2026: цепочка работает целиком

Первый прогон, в котором ассистента СЛЫШНО. Владелец подтвердил вслух, и это
попало в транскрипт: «Отлично, Роман, я тебя прекрасно слышу».

Замеры одного разговора:

| Что | Значение |
|---|---|
| Длительность | 366 с (6:06) |
| Реплик в транскрипте | 22 |
| Реплик ассистента (сегментов) | 13, от 3,0 до 18,1 с |
| Стоимость | $0,2722, списано 1226 токенов |
| Выход | по вердикту `empty` — после того, как встреча опустела и прошла выдержка |
| Бот | убран сразу на завершении звонка, `external_bot_id` очищен |
| Chrome в контейнере после встречи | 0 процессов |

Что подтвердилось в бою:

- **звук в обе стороны**: голос человека дошёл до модели и распознался,
  голос Романа слышен во встрече;
- **гейт по имени**: срабатывал на «Роман, …» и молчал в остальное время;
- **состав встречи**: `участников: 1 → наедине` сразу после впуска,
  `first_human_at` выставлен, звонок ушёл из `dialing` в `active` — то, что в
  прошлых прогонах ломалось;
- **специалисты**: `ask_specialist` дошёл до бэкенда (задача Александре про
  стратегию продвижения офлайн-бизнеса), ответ приехал и был озвучен, и
  решение попало в резюме звонка;
- **резюме и списание**: как на своих встречах, отдельного кода не
  потребовалось;
- **уборка**: бот вышел из встречи через две секунды после нашего выхода,
  Chrome не остался.

### Находка без исправления: словесной просьбы уйти ассистент не понимает

Владелец сказал: «Можешь завершить своё соединение» — и ничего не произошло.
Роман попрощался и остался во встрече до вердикта `empty`, то есть до того,
как из встречи вышел человек, плюс выдержка.

Это не сбой, а незакрытый сценарий: уйти ассистент умеет по опустевшей
встрече, по кнопке «Выйти» в карточке чата и по потолку длительности, а
голосом — нет. Просьба уйти вслух звучит естественно, особенно когда человек
уходить из встречи сам не собирается. Заводить отдельный инструмент выхода —
отдельная задача: нужно решать, кому позволено выгонять ассистента (любому
участнику или только хозяину встречи) и как отличить «Роман, выйди» от
пересказа чужих слов.

## Замеры: ресурсы, время входа, стоимость

Стенд: 4 vCPU, 7,9 ГБ RAM. Замер снимается скриптом `measure-meeting.sh`
рядом с входом бота.

### Вход в ОТКРЫТУЮ встречу — 16 секунд, без впуска

Прогон 10.09.2026 на встрече с включённым «Быстрым доступом». Состояния
`waiting_room` не было вовсе, кнопка нажата сразу:

| Момент (лог Attendee) | Время | От запроса |
|---|---|---|
| Запрос входа через API | 21:39:38 | — |
| Chrome начал грузить страницу Meet | 21:39:39,9 | +2 с |
| Нажата кнопка входа | 21:39:49,7 | +12 с |
| `Successfully joined meeting` | 21:39:54,4 | +16 с |
| Вебсокет звука на связи | 21:39:54,9 | +17 с |

Для сравнения: в закрытой встрече те же шаги упираются в ожидание человека, и
Attendee сдаётся через 10 минут (`request_to_join_denied`).

### Ресурсы одной встречи

| Фаза | RAM контейнера воркера | CPU | Chrome | Наши процессы |
|---|---|---|---|---|
| простой | 535 МиБ | 0,1 % | нет | 589 МБ |
| встреча, устойчиво | 1,11–1,17 ГиБ | 0,4–0,9 ядра, пики до 1,65 | 12 процессов | 754 → 808 МБ |
| через 10 с после выхода | 565 МиБ | 0,2 % | 0 процессов | 593 МБ |

- **Одна встреча стоит ≈570 МБ** в контейнере Attendee и **≈165 МБ** у нас.
- Chrome разворачивается в 12–14 процессов; сумма их RSS (1,3–1,45 ГБ) больше
  показаний контейнера, потому что дважды считает разделяемую память — верить
  надо счётчику контейнера (cgroup).
- **Всё возвращается после выхода**: Chrome исчезает, контейнер откатывается к
  565 МиБ (+30 МиБ кешей Django/Celery против исходных 535), наши процессы — к
  базовым 593 МБ. Утечки нет.
- Пик 1,65 ядра — на загрузке страницы Meet; дальше разговор держит 0,4–0,9
  ядра. По CPU на 4 vCPU влезло бы 3–4 встречи, но потолок в одну встречу
  задаёт не железо, а изоляция аудиоустройств (см. предупреждение о
  Celery-режиме в начале файла).
- **В тишине дешевле по деньгам, но не по железу.** Отдельный замер (бот один
  в встрече, 11 минут молчания): контейнер ровно 1,06–1,08 ГиБ **без роста**,
  CPU 0,33–0,61 ядра. Chrome продолжает рендерить страницу Meet, даже когда
  никто не говорит, — присутствующий бот стоит полъядра, и планировать надо по
  этой цифре, а не по «молчит — значит не грузит».

### Открытый дефект: процесс задания растёт ~12 МБ/мин, и это НЕ контекст

Замерено дважды: **15,4 МБ/мин** при активном диалоге (754 → 808 МБ за 210 с)
и **11,6 МБ/мин в полной тишине** (752 → 891 МБ за 720 с, бот один в встрече).

Второй замер и есть находка: в тишине контекст разговора не растёт вовсе,
значит причина не в накоплении транскрипта, как я решил по первому замеру.
Скорость совпадает с темпом, которым мы кладём кадры звука в поток, — 50
кадров в секунду по несколько килобайт дают примерно те же 12 МБ/мин.

Главный подозреваемый — отсутствие обратного давления: и `attendee-audio.ts`,
и `mixed-audio-input.ts` зовут `controller.enqueue()`, не глядя на
`desiredSize`, а у `ReadableStream` по умолчанию highWaterMark = 1 — очередь
растёт без предела, если потребитель отстаёт хоть немного.

**Это касается не только Meet.** Тот же приём в `mixed-audio-input.ts` — это
обычные звонки и встречи Taler ID. Наружу дефект до сих пор не вылезал по двум
причинам: всё освобождается при выходе задания, а двухчасовой потолок
ограничивает накопление (≈1,4 ГБ на потолке).

Не исправлено. Порядок работы: сначала подтвердить замером (вывести в лог
`desiredSize` и длину очереди), потом либо уважать обратное давление — для
звука реального времени правильно РОНЯТЬ кадр, а не проигрывать устаревший, —
либо найти того, кто держит ссылки на кадры.

### Проверено попутно: выход из пустой встречи

Бот, вошедший в встречу, где никого нет, вышел ровно по нашему правилу:
**901 секунда** при `LOBBY_MS` = 900 с, вердикт `never_started`. Таймер
одиночества Attendee (`only_participant_in_meeting_timeout_seconds`, у нас
300 с) при этом НЕ запускается — он требует, чтобы человек в встрече хоть раз
был (`number_of_participants_ever_in_meeting_excluding_other_bots() <= 1`
выходит из проверки досрочно). То есть за случай «бот пришёл, а встречи нет»
отвечаем мы, и никто больше.

### Стоимость

Модель — `gpt-realtime-2.1`; ставки в коде: **$32 за 1M входных аудио-токенов
и $64 за выходные** (`AUDIO_RATES_USD_PER_1M` в voice-call.service.ts).

| Прогон | Длительность | Реплик | Стоимость | В час |
|---|---|---|---|---|
| Тишина: бот один, только вступление | 901 с | 1 | $0,0161 | **≈$0,06** |
| Активный диалог (4-й) | 366 с | 22 | $0,2722 | **≈$2,68** |
| Активный диалог (5-й) | 332 с | 18 | $0,2351 | **≈$2,55** |

**Платится речь, а не время присутствия**: четверть часа тишины стоила полтора
цента. Реальная встреча ляжет между границами — модель слышит речь ВСЕХ
участников, а не только обращения к ассистенту, и входные токены тратятся
независимо от того, отвечает он или молчит по гейту.

Плата растёт нелинейно и внутри разговора: с его длиной растёт контекст,
который переотправляется на каждый ответ. Это вторая причина двухчасового
потолка.

Прежние оценки «$0,25–0,63 в час в режиме слушателя» из замеров 27.08.2026
здесь не применимы: они снимались на mini и на другом пути.

## Zoom: работает ТОЛЬКО веб-адаптер (спайк 10.09.2026)

Проверено на живой встрече владельца, приложением Zoom его же аккаунта.
Харнесс — `zoom-spike.cjs` (поднимает вебсокет, создаёт бота через API, шлёт во
встречу тон 440 Гц).

| Адаптер | Вход | Звук ОТ встречи | Звук ВО встречу |
|---|---|---|---|
| Нативный SDK — **значение по умолчанию** | да, с состоянием `waiting_room` | 20 572 куска, 24 кГц, RMS до 0,2427 | **тишина** |
| `zoom_settings: {"sdk": "web"}` | да | 24 кГц, идёт | **слышно**, микрофон включается сам |

**Главное: для Zoom обязательно передавать `zoom_settings: {"sdk": "web"}`.**
Нативный адаптер — дефолт, и если его не переопределить, ассистент войдёт во
встречу и будет немым.

Почему нативный не годится, и что именно исключено. Диагностическая правка
(`patches/zoom-unmute-diagnostic.py`, на стенде откачена) показала изнутри SDK:

```
on_mic_start_send_callback called
LINKEON mic: muted=False, can_unmute_by_self=True, audio_join_type=AudioType.AUDIOTYPE_VOIP
```

То есть бот не приглушён, сам может снять приглушение, находится в голосовом
канале (`JoinVoip()` мост вызывает), `audio_raw_data_sender.send()` принимает
каждый кадр с `SDKERR_SUCCESS` — ни одного `error with send_raw_audio` в
логе, — и во встрече при этом тишина. SDK принимает звук и не отдаёт его.
Дальше не копали: веб-адаптер работает, а он отдаёт звук той же браузерной
машинерией, что Meet.

### Что нужно, чтобы Zoom вообще создался

1. **Приложение в Zoom Marketplace**: Develop → Build App → General App, в
   Features включить Meeting SDK, взять Client ID и Client Secret.
2. Записать их в credentials проекта Attendee (тип Zoom OAuth). Без них
   создание бота отклоняется сразу:
   `HTTP 400 {"error":"Zoom App credentials are required to create a Zoom bot…"}`
   — ссылку на встречу мост при этом даже не смотрит.
3. **Неопубликованное приложение входит только во встречи того же аккаунта
   Zoom.** Для клиентских встреч приложение придётся проводить через ревью
   Zoom и публиковать — отдельный процесс на их стороне.

### Отличия от Meet, которые видно сразу

- **Комната ожидания приезжает состоянием** `waiting_room`, а не молчанием в
  `joining`, — значит плашка «ждём, пока впустят» на Zoom точна.
- **Микрофон бота включается сам** (видно и в списке участников: сначала
  перечёркнут, потом включается) — правка кнопки микрофона, обязательная для
  Meet, здесь не нужна: у веб-клиента Zoom своя разметка, и поиск моста по ней
  срабатывает.
- Ссылка Zoom не сводится к короткому коду: в ней хост аккаунта
  (`us04web.zoom.us`), числовой id и хеш пароля. Для разводки провайдера это
  значит, что в `external_room` надо хранить нормализованную ссылку целиком, а
  не «код», как у Meet.

### Продуктовый путь пройден 10.09.2026

После спайка провайдер `zoom` разведён по коду (контракт расширен полем `url`:
из числового id ссылку не собрать — нужен хост аккаунта и хеш пароля) и
проверен живой встречей через обычную ручку входа, а не через харнесс:

| Что | Результат |
|---|---|
| Запись звонка | `completed`, 142 с, 10 реплик, $0,0847, 382 токена |
| Резюме | «Обсуждали новости технологий. Ключевое из ответа: рост автон…» |
| Состав встречи | `участников: 1 → наедине` сразу после впуска, `first_human_at` выставлен |
| Гейт по имени | сработал на «Привет, Роман» и на «Расскажи про новости технологии» |
| Реплик ассистента | 6 сегментов, 5,7–18,4 с |

**Найдено и исправлено этим прогоном:** хозяин Zoom закрывает встречу для всех,
бот уходит в `ended`, и наш аварийный путь успевал записать «Звонок не
состоялся: бот Attendee: ended» — а уже потом приходило штатное завершение и
переписывало запись на `completed`. Повезло с порядком; не дожил бы воркер —
состоявшийся разговор остался бы «не состоявшимся». Теперь терминальное
состояние бота различается по тому, был ли во встрече человек: был — это конец
встречи, не был — отказ. `failed` снова означает «встреча не состоялась».

Гейт, специалисты, транскрипт и резюме от площадки не зависят и на Meet
отработаны раньше; на Zoom подтверждены этим же прогоном (кроме специалистов —
их в этом разговоре не звали).

## Синхронизация встречи в комнату LiveKit: спайк 10.09.2026

Проверялось `roomsync-spike.cjs` на живых встречах Teams. **Путь работает** и
снимает почти все костыли вебсокетной ветки.

Что подтвердилось:

| Что | Результат |
|---|---|
| Зеркалирование участников | Каждый участник встречи — ОТДЕЛЬНЫЙ участник LiveKit: `identity=8:koval_v`, `name=Владимир Коваль`, своя моно-дорожка |
| Звук по участнику | Живая речь на дорожке: RMS до **0,2622** (в прогоне с выключенным микрофоном — ровно 0,0000, то есть уровень настоящий, а не артефакт) |
| Обратный путь | Тон 440 Гц, опубликованный нами в комнату, **слышен во встрече** — мост берёт звук у участника, указанного в `source_participant.identity` |
| Чат | Зеркалится: «Привет Роман!», «Пишу еще одно сообщение!» пришли текстовым потоком на топик `lk.chat` |
| Teams | Входит анонимно по ссылке `teams.live.com`, без аккаунта Microsoft под бота |

Настройка: ключи LiveKit в credentials проекта (тип `LiveKit`), адрес —
достижимый **из контейнера**: на стенде `ws://172.17.0.1:7880`, а не
`ws://localhost:7880`, который для моста означал бы его собственный контейнер.

### Почему это важнее, чем ещё одна площадка

Наша вебсокетная ветка (`AttendeeAudioHub`) выросла в набор костылей, и почти
каждый из них здесь не нужен:

| Костыль вебсокетной ветки | Что с ним при синхронизации |
|---|---|
| Порт на задание и потолок в одну встречу | Не нужен: комната вместо порта |
| Переподключения, выдержка, пинг | Не нужны: соединение держит SDK LiveKit |
| Вранье о составе, выдержка 90 с, речь как доказательство присутствия | Не нужно: настоящие события участников LiveKit |
| Нет диаризации (микс) | Есть: дорожка на участника |
| Перебивание без сброса очереди | Темп держит `AudioSource.captureFrame`, как в своих комнатах |
| Рост памяти ~12 МБ/мин в нашем конвейере | Конвейер заменяется тем, что уже работает на Taler ID |

Плюс появляется чат встречи как канал: Роман сможет и читать вопросы из чата,
и кидать туда ссылку на подготовленный документ.

Потолок в одну встречу это НЕ снимает целиком: изоляция аудиоустройств в
Celery-режиме остаётся ограничением моста (см. предупреждение в начале файла).

### Итог: перенос сделан и ОТКАЧЕН (11.09.2026)

Meet и Zoom были переведены на этот путь целиком — с удалением нашего
вебсокет-хаба, `presence.ts` и вебхуков состава, — и возвращены обратно после
трёх живых встреч. Причина одна: **исходящий звук в Google Meet не работает**.

Что работало на новом пути (проверено живьём):

- участники встречи зеркалились отдельными участниками LiveKit со своими
  дорожками, микшер их читал (`[вход] кадров от участников` рос ровно);
- гейт по имени срабатывал на обращения, транскрипт шёл, `first_human_at`
  выставлялся, выход по опустевшей комнате отрабатывал;
- ветка воркера схлопнулась в ветку своих комнат — кода стало заметно меньше.

Чего не было: ассистента во встрече **не слышно**, при том что каждое звено
по отдельности отчитывалось успехом:

| Звено | Что говорил лог |
|---|---|
| Дорожка агента в комнате | `mediaTrack published`, подписчик моста `attendee-source-subscriber-…` в комнате (лог SFU) |
| Страница бота получила дорожку | `LiveKitTrackAdded` с identity агента |
| Микрофон бота | `LinkeonMicToggle found: True`, сторож держит включённым; владелец видел включённый значок |
| Соединение с графом микрофона | `LinkeonAudioToMic ok: True, ctxState: running` |
| Обходной приём Chrome | `sinkPlay: True` — поток проигрывается беззвучным элементом |

**Насколько удалось установить причину.** Адаптер моста отдаёт полученный
поток через `playBotOutputMediaStream("webcam")` — то есть через ВЕБ-КАМЕРУ
бота, а камеру Attendee в Meet при входе выключает. Микрофон при этом несёт
другую дорожку: `sourceAudioTrack` из внутреннего аудиографа страницы, куда
звук из комнаты не попадает. Наша правка
(`patches/roomsync-audio-to-mic.py`) соединяла дорожку из комнаты с этим
графом и дополнительно «проигрывала» поток — оба шага отчитались успехом, и
звука всё равно не было.

**Непроверенная гипотеза для того, кто вернётся к задаче:** раз звук едет
вместе с видеопотоком камеры, возможно, достаточно НЕ выключать камеру бота в
Meet (тогда он будет виден чёрным квадратом — или с аватаром через
`output_image`). Мы этого не пробовали: договорённость с владельцем была одна
правка — одна проверочная встреча, и она была израсходована.

**На Teams путь работает целиком** — тон из комнаты слышен во встрече. То есть
дело не в самой синхронизации, а в том, как мост доставляет звук именно в
Meet.

Ветка вернулась на вебсокетный путь, живьём подтверждённый и на Meet, и на
Zoom. Из этой работы сохранилось одно исправление: `syncSolo` теперь считает
РОВНО одного участника — пустая комната больше не читается как разговор
наедине, и гейт в ней не срывается в свободный режим. Дефект был и до
переноса.

### Наложение прогонов ломает синхронизацию — процедурное ограничение

Третий прогон дал ноль: ни участников, ни чата, тон не слышен. Причина не в
пути. Лог SFU показал прямо: в комнату второго прогона подписчик моста
(`attendee-source-subscriber-…`) приходил, а в комнату третьего — **нет
вовсе**. В контейнере в этот момент доживал бот предыдущего прогона: он вышел
по своему таймеру одиночества и в 19:57:13 закрыл «LiveKit WebSocket bridge»
ровно тогда, когда новый бот входил (19:57:16).

То есть в Celery-режиме боты делят не только аудиоустройства, но и машинерию
синхронизации. **Перед каждым прогоном надо убеждаться, что живых ботов
нет** — `GET /api/v1/bots` и `leave` всем незавершённым. Четвёртый прогон,
запущенный на чистом контейнере, прошёл полностью.

Для продукта это значит, что потолок в одну встречу синхронизация НЕ снимает:
он лишь перестаёт держаться нашим портом и начинает держаться этим
ограничением моста.

### Три ошибки спайка, все мои

Записаны, потому что обе — про то, как легко получить ложный отрицательный
результат:

1. **Тон по таймеру.** Публиковался через двадцать секунд после старта, а бот
   входил пятьдесят пять — тон ушёл в пустоту, и первый прогон закончился
   выводом «обратный путь не проверен». Теперь тон уходит по событию
   `joined_recording` и повторяется трижды.
2. **Чат слушался не тем каналом.** Мост зеркалит его текстовым потоком
   (`send_text` на топик `lk.chat`), а спайк слушал только `DataReceived` — и
   отчитался «чат не зеркалится», хотя мост его поймал (десять упоминаний
   `ChatMessage` в логе контейнера). Теперь зарегистрирован
   `registerTextStreamHandler('lk.chat', …)`.
3. **Прогон поверх недоубранного бота.** Отчитался «ничего не зеркалится»,
   хотя мешал предыдущий бот в том же контейнере (см. раздел выше). Отсюда
   правило: сначала убедиться, что живых ботов нет, потом запускать.

Общее у всех трёх: ложный отрицательный результат из-за проверки, смотрящей
не туда. Дороже всего он стоит тогда, когда для проверки нужен живой человек
на встрече, — а именно так и проверяются мосты.

## Установка

Опубликованного образа **нет** — собирается из исходников. Postgres и Redis
Attendee поднимает своим compose, наши трогать не нужно.

```bash
git clone https://github.com/attendee-labs/attendee.git
cd attendee
# Зафиксировать коммит: Attendee ходит по живой вёрстке Meet, и обновление
# «когда-нибудь потом» ломает встречу в неожиданный момент.
git log --oneline -1   # записать sha сюда, в этот файл

docker compose -f dev.docker-compose.yaml build          # ~5 минут

# Сгенерировать .env
docker compose -f dev.docker-compose.yaml run --rm attendee-app-local \
  python init_env.py > .env

# Дописать в .env хранилище записей и продовые настройки (см. ниже)
$EDITOR .env

docker compose -f dev.docker-compose.yaml up -d
docker compose -f dev.docker-compose.yaml exec attendee-app-local \
  python manage.py migrate
```

Дальше через UI на `:8000`: создать аккаунт (письмо подтверждения приходит **в
логи контейнера**, не на почту), войти, в разделе «API Keys» выпустить ключ.

### Что дописать в `.env` Attendee

`init_env.py` генерирует основное. Руками добавляются:

- **Хранилище записей** — в инструкции это AWS S3. Записи нам не нужны, но
  переменные требуются. Подставить наш MinIO (он уже есть на проде и в
  `docker-compose.minio.yml`), а не заводить AWS: лишний платный аккаунт ради
  функции, которой мы не пользуемся.
- Оставить `LAUNCH_BOT_METHOD` пустым (Celery-режим) — осознанно, см. шапку.

### Что дописать в наш `.env`

Бэкенд (`~/spirits_back/.env`):

```env
ATTENDEE_BASE_URL=https://<хост attendee>
ATTENDEE_API_KEY=<из UI>
ATTENDEE_WEBHOOK_SECRET=<из UI, раздел вебхуков>
ATTENDEE_WEBHOOK_URL=https://my.linkeon.io/webhook/meet/attendee
```

Воркер (`~/spirits_back/voice-host/.env`):

```env
ATTENDEE_WS_PORT_MIN=8140
ATTENDEE_WS_PORT_MAX=8140
ATTENDEE_WS_PUBLIC_BASE=wss://my.linkeon.io
```

`ATTENDEE_AUDIO_WS_URL` не нужен: адрес вебсокета собирает воркер сам — порт
свой у каждого задания.

### Nginx на проде

Положить `nginx-attendee.conf` в конфиг сайта `my.linkeon.io`, заменив
`ATTENDEE_HOST_IP` на адрес хоста Attendee. Диапазон в регулярке обязан
совпадать с `ATTENDEE_WS_PORT_MIN/MAX`.

## Проверка

```bash
# API отвечает
curl -s -o /dev/null -w '%{http_code}\n' https://<хост attendee>/

# Ключ рабочий
curl -s https://<хост attendee>/api/v1/bots \
  -H "Authorization: Token $ATTENDEE_API_KEY" | head -c 200

# Наш вебсокет достижим снаружи через nginx (ожидается 400/426, не 502:
# это значит, что nginx дошёл до порта, а там нет апгрейда)
curl -s -o /dev/null -w '%{http_code}\n' https://my.linkeon.io/attendee/8140
```

## Обновление

Attendee ходит по живой вёрстке Google Meet, поэтому обновляться придётся —
но осознанно, а не по `latest`:

1. Проверить активные встречи: `SELECT count(*) FROM voice_calls WHERE
   provider='meet' AND status IN ('dialing','active')`.
2. `git fetch && git checkout <новый sha>`, пересобрать, накатить миграции.
3. Провести живую встречу до того, как объявить обновление успешным: сборка и
   миграции не проверяют, что бот всё ещё умеет входить в Meet.
4. Записать новый sha в этот файл.

## Смежное

- Спека: `docs/superpowers/specs/2026-09-06-meeting-bridge-meet-design.md`
- План: `docs/superpowers/plans/2026-09-07-meeting-bridge-meet.md`
- Наша сторона моста: `voice-host/src/attendee-audio.ts`,
  `src/meeting/attendee.client.ts`, `src/meeting/meet-webhook.controller.ts`
